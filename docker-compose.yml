services:
  trainer-bot:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - PORT=8080
      - STATUS_TOKEN=your-secret-token
      - DATA_SOURCE=coingecko
    restart: unless-stopped
